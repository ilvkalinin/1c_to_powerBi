# Реестр отсутствующих объектов источника

Назначение: полный перечень физических объектов, отсутствующих в фактической
базе `gymdb.public` и относящихся к договорным отчётам, для одного обращения к
системному администратору. Реестр пополняется только доказательствами из
server validation; не заменяет исходные таблицы альтернативными именами без
проверки.

Правило пользователя от 2026-08-11: при каждом новом доказанном отсутствии
физического объекта сразу дополнять этот реестр и единый запрос системному
администратору; не создавать для такого объекта отдельное обращение.

Дата актуализации: 2026-08-27.

## Единый запрос системному администратору

Запрос исполнен и подтверждён read-only проверкой SV-090 от 2026-08-13.
Исторические строки ниже сохранены как единственный реестр доказанных
физических отсутствий. Они не означают, что объект по-прежнему отсутствует.
После поставки выполняются только metadata и кардинальные проверки; пустые
заглушки и альтернативные объекты не принимаются как замена.

| Логический объект 1С | Ожидаемое физическое имя | Назначение в contract scope | Историческое доказательство отсутствия | Статус поставки |
|---|---|---|---|
| `Document298.VT3596` | `_document298_vt3596` | Текущий SQL «Отчёта по промокодам»: связь скидки с маркетинговой акцией. Без таблицы невозможно воспроизвести результат. | SV-006; `docs/reports/promo_codes_query_review.md` | `VALIDATED AVAILABLE` — SV-090: ordinary table, 7 physical columns |
| `Document294` | `_document294` | Кандидат кассира для «Выручки рецепции». Текущий факт его не использует, но пользователь потребовал включить все уже доказанные отсутствующие источники в один запрос. | SV-006; `docs/reports/reception_revenue_query_review.md` | `VALIDATED AVAILABLE` — SV-090: ordinary table, 28 physical columns |
| `Document275` | `_document275` | Документ возврата: 194 строки `AccumRg7575` за 2026 относятся к этому отсутствующему регистратору; нужен для проверки возвратной ветки. | SV-014 | `VALIDATED AVAILABLE` — SV-090: ordinary table, 22 physical columns; 1 576/1 576 current `AccumRg7575` Document275 recorder rows matched |
| `Document315.VT3887` («ДополнительныеПакеты») | `_document315_vt3887` | Единственный metadata-кандидат line-level связи возвратного `Document315` с исходным чеком, ребёнком и ключом строки для «Продаж детских пакетов». | CPS-RETURN-RECORDER-001, 2026-08-27: relation отсутствует в `information_schema.columns`; `_document315` физически доступен. | `NOT AVAILABLE` — не подменять связь данными основного документа или group-level регистра. |
| `Document275.VT3122` («Запасы») | `_document275_vt3122` | Кандидат line-level ключа и исходного документа в возврате покупателя для «Продаж детских пакетов». | CPS-RETURN-RECORDER-001, 2026-08-27: relation отсутствует в `information_schema.columns`; `_document275` физически доступен. | `NOT AVAILABLE` — без табличной части нельзя распределить возврат между несколькими детьми одной sales-group. |
| `Enum471` («Статусы взаимодействий») | `_enum471` | Полная самостоятельная расшифровка домена `Reference67.Fld830`. | CR-V05F/G; 2026-08-18: `pg_class` не нашёл relation с именем `enum471`, прямой join к `_enum471` вернул `UndefinedTable`. | `NOT AVAILABLE` — не заменять другой таблицей или порядком перечисления. Для трёх текущих статусов «Отчёта по обращениям» это не блокер: exact mapping зафиксирован в сохранённом current SQL, а CR-V05F/G подтвердил структуру трёх non-NULL значений. |
| `Enum526` («Типы оплат абонемента») | `_enum526` | Полная расшифровка всех значений `Reference59.Fld699` в «Маркетинговой воронке». | MF-V07B; 2026-08-18: `pg_class` не нашёл relation с именем `enum526`. | `NOT AVAILABLE` — не выбирать значения по частоте, порядку или предполагаемому значению ID. Классификация «рекарринг/предоплата» больше не блокируется: exact правило подтверждено текущим M-кодом поступлений и закреплено в BR-024. |
| `AccumRg7575.RecordKind` | `_accumrg7575._recordkind` | Наблюдение record kind в NV-V05 для «Новичков и гостевых визитов». | NV-V05 metadata control 2026-08-18: колонка отсутствует; среди полей регистра подтверждены `_active`, `_fld7585`, `_fld7579rref` и `_period`. | `NOT AVAILABLE` — не подбирать substitute. Первый релиз определяет знак ACCUNIQ только согласованным правилом `Fld7585 = -1 → -1`, иначе `+1`; NV-V05 наблюдает `_active` и не использует несуществующее поле. |

## Полнота списка

SV-006 сверил 94 physical dependencies, извлечённые из mapping договорных
отчётов: 91 таблица найдена, две зависимости из перечня выше отсутствовали, а
`Reference141` подтверждён как физический alias `_reference141x1`. SV-014
отдельно обнаружил отсутствующий `Document275` среди реальных типов
регистраторов. SV-090 подтвердил поставку трёх исторически отсутствующих
объектов. CR-V05 и MF-V07 добавили `Enum471` и `Enum526` как текущие
непоставленные physical dependencies для label mapping CRM-статусов и типов
оплаты. Это не завершает business- или кардинальные проверки остальных
объектов.

`Document315.VT3887` и `Document275.VT3122` добавлены после доказательства
CPS-RETURN-RECORDER-001: они нужны для проверки line-level attribution
возврата в «Продажах детских пакетов». Другие табличные части из полного
metadata 1С по-прежнему не включаются без отдельного evidence.

`Reference141` не является отсутствующим объектом: его физическая реализация
в этой базе — `_reference141x1`.
