# Source-to-target mapping: выручка рецепции

Статус: `BUSINESS MAPPING COMPLETE / STAGE 2 VALIDATED / IMPLEMENTATION DEFERRED`.

SQL и физические объекты не создаются в рамках текущего этапа.

Архитектурное направление после mapping зафиксировано в
[`ADR-0005`](../adr/0005-shared-ancillary-revenue.md); реализация требует
отдельного разрешения на Stage 3.

## Компонент A: факт

Предварительная гранулярность:

> дата движения × клуб × сотрудник × номенклатура × вид деятельности ×
> категория рецепции × тип исходного регистра.

Логический ключ:

> `(revenue_date, club_id, employee_id, service_id, activity_id,
> reception_category_key, source_kind)`.

До агрегации кандидат технического ключа:

> `(source_kind, Recorder, LineNo)`.

| Целевая колонка | Бизнес-описание | Источник / преобразование | Тип PostgreSQL | NULL | Статус / доказательство | Проверка |
|---|---|---|---|---|---|---|
| `revenue_date` | дата факта | `AccumRg7575.Period::date` / `AccumRg7646.Period::date` | текущая календарная ось M/DAX: `_Period::date`; исходную семантику сохраняет `source_kind` | `date` | нет | CONFIRMED — решение пользователя 2026-07-30 | диапазон и timezone |
| `source_kind` | регистр-источник | константа `visit/sale` | `smallint`/`text` | нет | CONFIRMED BY DESIGN | пересечение |
| `club_id` | клуб движения | `Fld7577` / `Fld7653` | UNKNOWN | нет | CONFIRMED metadata | orphan |
| `employee_id` | текущий сотрудник строки для рейтинга | `AccumRg7575.Fld7582` / `AccumRg7646.Fld7652`; отображаемое имя берётся отдельным справочником сотрудников | `bytea` в источнике; целевой тип уточняется при реализации | нет в текущем рецепционном scope | CONFIRMED current rule / SV-051 | уникальность движения, NULL, сравнение с чеком |
| `service_id` | номенклатура | `Fld7579` / `Fld7649` | UNKNOWN | нет | CONFIRMED metadata | orphan |
| `activity_id` | вид деятельности | `Reference163.Fld1733 → Reference70.ID` | UNKNOWN | да | CONFIRMED metadata | scope |
| `reception_category_key` | текущая категория рецепции | последовательная текущая M-классификация по названию номенклатуры и виду деятельности; все 53 666 строк контрольного среза классифицированы (SV-052) | `text` | нет | CONFIRMED current rule | полнота, взаимоисключаемость и контроль сумм |
| `sold_quantity` | знаковое количество | `SUM(Fld7585)` / `SUM(Fld7657)` | `numeric` | нет | CONFIRMED current | единицы и знак |
| `revenue_amount` | знаковая выручка | `SUM(Fld7586)` / `SUM(Fld7659)` | `numeric` | нет | CONFIRMED current | сумма и знак |

## Компонент B: план

План остаётся внешней Excel-таблицей и используется как есть. Его физическая
структура не переносится в PostgreSQL.

| Целевая колонка | Бизнес-описание | Источник | Тип | NULL | Статус |
|---|---|---|---|---|---|
| `plan_date` | дата дневного плана | Excel `План` | `date` | нет | EXTERNAL / остаётся как есть |
| `club_id` | клуб | Excel `План` | UNKNOWN | нет | EXTERNAL / остаётся как есть |
| `activity_id` | вид деятельности | Excel `План` | UNKNOWN | нет | EXTERNAL / остаётся как есть |
| `planned_revenue` | дневной план | Excel `[План на день]` | `numeric` | нет | CONFIRMED DAX / EXTERNAL |

План не присоединяется к строкам факта. PBIT подтверждает текущие активные
однонаправленные связи плана с общими датой, клубом и видом деятельности.
Excel-файл и его запросы остаются без анализа и изменений по решению пользователя.

## Повторное использование

Оба исходных регистра уже входят в `docs/mappings/dpfu_revenue.md`. Кандидат —
общий факт движений дополнительных услуг и товаров, а не отдельные копии
«ДПФУ» и «Рецепция». Для рецепции общий факт должен сохранить `employee_id` и
текущую последовательную классификацию услуги.

Прямое объединение с «Записями администраторов» отклонено: там сотрудник —
автор записи и другая гранулярность.

## Не переносить

- код, ФИО и дату рождения клиента;
- признак клиента;
- тип оплаты;
- код и название контракта;
- полное имя сотрудника внутри факта;
- текстовые названия клуба/услуги как ключи;
- среднюю цену, долю, прогноз и накопительные итоги;
- сырые регистры на VM-2;
- отдельные факты для пяти страниц.

## Методологические пометки и условия Stage 3

1. Семантика продавца рецепции для 7575: `Fld7582` — подтверждённый текущий
   ключ сотрудника, но не доказанный продавец. Подмена `Document346.Fld4909`
   меняет 2 518 строк и допускается только как отдельное улучшение методики.
2. Устойчивое ID-правило категории вместо текущей текстовой M-классификации —
   методологическое улучшение; первый релиз сохраняет текущую последовательность.
3. Производительность и способ физической реализации проверяются только в
   Stage 3 после отдельного разрешения.

Обновление: один раз в день.
