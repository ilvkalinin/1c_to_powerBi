# Разбор текущих запросов: «Посещения Пушкинский»

Статус: `EVIDENCE AND MAIN DAX REVIEWED`.

Источник: `docs/source_reports/visits_pushkinsky/Посещений Пушкинский.docx`, SHA-256 `7adf4a45451deed56b6a16a7064a8198444578bebcaade77ed2efffe17e9a799`.

## Наборы Power Query

| Набор | Источники | Текущий смысл | Основной риск |
|---|---|---|---|
| `Абонементы всего` | `Reference59`, клиент, клуб | абонементы с основным клубом Пушкинский | только дата окончания и исключение типа; нет динамического active interval/status |
| `Посещения всего` | `AccumRg7575`, `Document325`, абонемент, клуб | клиентский набор: фактический и основной клуб Пушкинский | название говорит «всего», но исключает не-членов и зависит от основания-абонемента |
| запрос купонов без явного имени | `InfoRg7006`, `Document329`, `Document325`, номенклатура | прибывшие по купонным услугам в Пушкинском | сложное сопоставление и последняя карта; риск many-to-many |
| `Посещения ГП` | `Document279`, `InfoRg8675` | сумма посетивших ГП в Пушкинском | готовый агрегат без client ID; `Marked` не проверяется |
| `Посещения платно ДПФУ` | `AccumRg7575` + `AccumRg7646` | альтернативный набор услуг и продаж | не используется предоставленной итоговой мерой; кандидат на удаление после проверки зависимостей |
| `Посещения проверка ДПФУ` | `AccumRg7575`, занятия/записи | услуги для основного и фактического клуба Пушкинский | именно эту таблицу использует DAX `Посещения проверка ДПФУ` |
| `Посещения ДРЦ` | посещения + абонемент | фактический Пушкинский, основной клуб ДРЦ | условие исключения Продлёнки написано через `NOT LIKE A OR NOT LIKE B` и практически всегда истинно |
| `Посещения продленка` | те же | ДРЦ и услуга по подстроке Продлёнки | классификация по тексту |
| `Посещения умняши` | те же | ДРЦ и услуга по подстроке `умняши` | классификация по тексту |
| `Посещения ГВ` | посещения + услуга | гостевые визиты в Пушкинский | широкий `ILIKE '%гост%'`, исключается только `гость кафе` |
| `Посещения ВИП` | посещения + абонемент | основной клуб VIP, фактический Пушкинский | исключение сотрудников по тексту услуги |

## Общие источники с «Посещения Физкульт»

- `AccumRg7575` — посещения;
- `Document325` — документ посещения;
- `InfoRg7006` и `Document329` — предварительная запись/купоны;
- `Document279` и `InfoRg8675` — ГП;
- `Reference59` — абонемент и основной клуб;
- `Reference132` — фактический/основной клуб;
- `Reference163` и `Reference70` — услуга и бизнес-направление.

Дополнительный источник `AccumRg7646` документирован metadata как `РегистрНакопления.Продажи`.

## Критические наблюдения

1. Нижние даты `2025-10-01` и `2025-11-01` захардкожены.
2. `Active`, `Posted` и `Marked` используются непоследовательно.
3. Применяются функции к датам и `encode()` в joins/filters.
4. Классификация ДРЦ/Продлёнки/Умняшек основана на тексте услуг.
5. Условие ДРЦ для двух вариантов написания Продлёнки логически ошибочно: нужен `AND`, а используется `OR`. Пользователь подтвердил исправление: Продлёнка не должна входить в ДРЦ; Умняшки также остаются отдельной категорией.
6. Два запроса ДПФУ имеют разную семантику; предоставленный DAX выбирает `Посещения проверка ДПФУ`.
7. Факт посещения, основной клуб доступа и активность абонемента на дату смешаны внутри запросов.
8. Основные посещенческие меры считают distinct клиентов и несуммируемы по датам.
9. `Итого КБ` раскладывается на `Посетили ЧК + Активная база + Неактивная база`; мера `Посетили ЧК на дату` подтверждена как distinct членов с посещением строго в D.
10. `Итого посещения` складывает категории без дедупликации между ними; ошибка `OR` в ДРЦ может завышать итог.

После исправления ДРЦ/Продлёнка/Умняшки должны быть взаимоисключающими. Остальные категории продолжают складываться по подтверждённым DAX-формулам.

Формула `Посетили ЧК на дату` получена: distinct клиентов таблицы членских посещений строго в D. Границы членства целевым образом унифицированы со снимком 00:00 отчёта «Клиентская база», хотя текущий DAX `Всего КБ` использовал другие неравенства.

## Read-only source validation — 2026-08-11

SV-071 подтвердил согласованную границу на 2026-07-15: 3 152 клиента КБ,
включая 12 контрактов с окончанием D−1 и исключая 25 с началом D. Три
когорты сходятся без остатка: `609 + 1 648 + 895 = 3 152`. Текущая member
visit-ветвь имеет 15 461 уникальное техническое событие, 15 068 client-day;
в проверенном срезе не найдено inactive/unposted/marked строк.

На июле ГП Пушкинского — 555 занятий и 4 585 посетивших без join excess.
Купоны сохраняют наблюдаемую M-кратность: 106 matched-строк на 101 event
(пять лишних строк), 89 client-day. ДРЦ: legacy и исправленное правило дают
по 168 строк, поскольку Продлёнки в срезе нет; 23 Умняшки выделены отдельно.
Это проверяет эффект правила, но не отменяет обязательное `AND` в будущем SQL.

## Направление оптимизации

- один общий client-day набор посещений для Физкульт и Пушкинского, без отдельных копий;
- source-side классификация категорий и устранение технических дублей;
- один общий дневной агрегат `дата × клуб`, объединяющий ГП и готовую структуру КБ Пушкинского;
- никакой полной копии абонементов, продаж или посещений на VM-2.
