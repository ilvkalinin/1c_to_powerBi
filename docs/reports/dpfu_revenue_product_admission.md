# Stage 3 PRODUCT ADMISSION: `mart.ancillary_revenue_movement`

Статус: `IN PROGRESS — load and reconciliation review pending`.

## Подтверждённая граница

Пользователь подтвердил пакет `STAGE_3_PRODUCT_ADMISSION` 2026-08-13. Его
единственный продукт — `mart.ancillary_revenue_movement`: одно
квалифицированное движение `AccumRg7575` или `AccumRg7646` с ключом
`(source_kind, recorder_id, line_no)`.

В пакет не входят `mart.ip_training_daily`, `mart.ip_revenue_daily`,
`mart.dpfu_plan_assignment`, внешние Excel-источники, report-specific views
или выполнение DDL/DML.

## Закрытый вопрос: стабильный ключ клиента

`client_key` требуется в факте ДПФУ для `DISTINCTCOUNT` УЧК и должен быть
стабильным между датами, клубами и refresh. Его физический источник уже
подтверждён SV-054: `AccumRg7575.Fld7576` и `AccumRg7646.Fld7648` покрыты на
100%.

- `S3-ADMISSION-001`, выполненный 2026-08-13 в `BEGIN READ ONLY`, проверил
  `Reference141X1._Code` на точном квалифицированном scope ДПФУ;
- для 41 682 distinct client ID найдены 41 682 reference-строки, 0 null,
  0 пустых и 0 повторяющихся `_Code`;
- первый релиз использует `Reference141X1._Code::text` одновременно как
  `client_key` и `client_code`; исходный `bytea` ID остаётся на VM-1 и не
  передаётся в Power BI.

Дополнительно S3-ADMISSION-002 подтвердил ключ движения: в текущем scope
ДПФУ 813 670 строк и 0 повторов как по
`(source_kind, RecorderRRef, LineNo)`, так и по более длинному ключу с
`RecorderTRef`. S3-ADMISSION-003 подтвердил типы: `LineNo numeric(9,0)`,
количество `numeric(15,3)`, выручка `numeric(15,2)`.

## Закрытый технический риск: объединение source extract

S3-ADMISSION-004, выполненный 2026-08-13 в `BEGIN READ ONLY`, показал, что
две ветви при независимом выполнении дают ожидаемый квалифицированный набор:

| Ветка | Строки | Количество | Выручка |
|---|---:|---:|---:|
| `AccumRg7575` | 812 793 | 825 550,000 | 953 835 414,35 |
| `AccumRg7646` | 892 | 886,000 | 47 497,50 |

Кандидатный объединённый CTE при проверке вернул другой набор строк, поэтому
он отозван и не используется. Безопасная реализация сохраняет семантику
`UNION ALL`: runner читает две подтверждённые ветви отдельными source-query в
одном `REPEATABLE READ, READ ONLY` snapshot и добавляет обе в одну временную
таблицу VM-2 без дедупликации. Перед заменой факта runner сравнивает сумму
строк, количества и выручки каждой ветви. Это не меняет состав строк, а
исключает непроверенный объединённый запрос.

## Подтверждённая целевая VM-2

S3-ADMISSION-005, выполненный 2026-08-14 в read-only соединении, подтвердил
целевую БД `fitness_dwh`: схема `mart` и таблица
`mart.ancillary_revenue_movement` физически отсутствовали; роль имеет право
создать схему и является владельцем базы.

Для review подготовлен [проект DDL](dpfu_revenue_ancillary_movement_ddl_review.sql).
Он создаёт ровно одну схему `mart` и пустую таблицу факта с согласованными
ключом, типами и ограничениями. После отдельного разрешения пользователя DDL
выполнен 2026-08-14. S3-ADMISSION-006 в новом read-only соединении подтвердил
наличие схемы и таблицы, 19 колонок, 5 именованных ограничений и 0 строк.
Данные 1С не передавались.

## Следующее решение

Следующий обязательный артефакт — отдельный review загрузки и reconciliation:
две source-side ветви в одном read-only snapshot, временная таблица VM-2,
атомарная замена горизонта BR-003 и контроль сумм. Любые DML остаются
запрещены до нового отдельного разрешения пользователя.
