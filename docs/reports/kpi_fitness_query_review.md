# Query review: «KPI Фитнеса»

Статус: `REVIEW COMPLETE / TECHNICAL VALIDATION COMPLETE`. Рассмотрены полный M/SQL/DAX из
`KPI фитнеса.docx` и бизнес-описание из `KPI Фитнеса (2).docx`.

## Текущий конвейер

1. M объединяет `AccumRg7575` и `AccumRg7646`, присоединяет клиента, клуб,
   услугу, подразделение, формат, тренера и контракт, затем формирует
   возрастные/кластерные/расчётные классификации.
2. Отдельно M формирует выручку ИП из `AccumRg7370`, тренировки ИП — из
   `InfoRg7006` и ПЗ/ГП, а текущие планы — из `InfoRg6612`.
3. Внешние таблицы дают годовой бюджет, план по дням, мероприятия и часть
   классификации услуг.
4. DAX считает контекстные distinct, средние месячные, прогноз, сравнения,
   доли, план-факт и факторы.

## Подтверждённые извлечения

| Компонент | Текущее правило | Статус |
|---|---|---|
| Штатные услуги | `AccumRg7575`: номенклатура-услуга, исключены членство/прочее членство/«посещение клуба», включён список фитнес-направлений | CONFIRMED current SQL |
| Прочие услуги | `AccumRg7646`: исключены услуга/абонемент/подарочный сертификат, затем те же направления | CONFIRMED current SQL |
| ИП | выручка — `AccumRg7370` с `RecordKind=0`; тренировки — отдельные ветви ПЗ/ГП `InfoRg7006`, исключены enum order 2, 3 | CONFIRMED current SQL |
| Текущий план | `InfoRg6612`: день, клуб, подразделение, тренер, клиент и сумма `Fld6620` | CONFIRMED current SQL |
| Месячные метрики | DAX: УЧК — среднее monthly distinct; регулярность = количество периода / (среднее monthly УЧК × число месяцев), то есть взвешенное отношение | CONFIRMED DAX / сохраняется в первом релизе по BR-018 |

## Существенные проблемы текущего M/SQL

| № | Наблюдение | Влияние | Целевое действие |
|---:|---|---|---|
| 1 | В обеих ветках нет доказанных фильтров активности, проведения, удаления и сторно. | Возможна неверная сумма/количество. | Проверить технические статусы до реализации. |
| 2 | Технический ключ движения и уникальность после joins не выбраны. | Риск размножения выручки и количества. | Валидировать `(Recorder, LineNo)` и каждый join. |
| 3 | `SELECT *` используется внутри CTE, а классификации по тексту смешаны с извлечением. | Неустойчивый контракт и лишняя передача данных. | Явный список колонок; классификацию фиксировать в mapping. |
| 4 | LAG-колонки имеют подтверждённого потребителя Renew. В июле 2026 неуникальный порядок не изменил 2 733 пары клиент–клуб, но старт истории PBIT исключает декабрь 2024. | Renew января 2025 не видит 3 607 фактических продлений. | Сохранять текущую границу PBIT по BR-018; технический предыдущий месяц — отдельное улучшение. |
| 5 | Текущий M переносит ФИО, дату рождения и контракт. | Лишние персональные данные на VM-2. | Оставить только код клиента для детальной страницы; ключ скрыть. |
| 6 | ИП-выручка обрезается до месяца. | Некорректен частичный месяц и дневные страницы. | Сохранять дату оплаты. |
| 7 | Планы и факт имеют разные grain. | Join до агрегации размножит факт. | Отдельные наборы, объединение мерами Power BI. |
| 8 | Внешняя группировка услуг и бюджеты не приложены. | Нельзя доказательно воспроизвести структуру и плановые KPI. | Зафиксировать как внешние dependencies. |
| 9 | Текущий DAX по-разному исключает аренду/ИП в выручке, количестве и среднем чеке. | Нужен единый явно именованный набор базовых мер. | Сохранить подтверждённые исключения и задать меры от классификационных полей. |
| 10 | `InfoRg6612` в части M считает строки клиентов, но DAX использует из текущего плана сумму выручки. | Возможна ложная метрика плана количества. | Из текущего плана использовать только `Fld6620`; план количества — внешний бюджет. |
| 11 | ПЗ ИП делает left join к `Document329_VT4352`, хотя условие уже требует строку VT. | 48 973 исходных события превращаются в 49 200 строк; превышение 227 (0,46%). | Сохранить exact legacy-кратность PBIT по BR-018; dedupe — отдельное улучшение. |

## Решение по переносу логики

В PostgreSQL после технической валидации: source-side фильтрация, квалификация
движений, минимальные ключи/измерения, агрегирование и устранение дублей. В
Power BI: `DISTINCTCOUNT`, средние месячные, прогноз, план-факт, доли,
сравнения с ПГ, рейтинг и индикация. Это сохраняет filter-dependent семантику
и исключает повторные тяжёлые raw-запросы.

## План read-only валидации

Выполненные SQL и контрольные значения — в
[`server_validation_2026-08-05.md`](../source_metadata/server_validation_2026-08-05.md),
SV-058—SV-061; исполняемый набор —
[`validation_sql/kpi_fitness_2026-08-11.sql`](../source_metadata/validation_sql/kpi_fitness_2026-08-11.sql).
Внешние Excel/Google Sheets по решению пользователя не входят
в серверную валидацию.

| ID | Проверка и ожидаемый результат |
|---|---|
| KF-V01 | `VALIDATED` — ключ движений уникален в обеих ветвях (SV-054). |
| KF-V02 | `VALIDATED` — покрытие клиента, клуба, услуги и подразделения; display-поля сотрудника/формата nullable (SV-054). |
| KF-V03 | `VALIDATED` — активность, возвраты, `RecordKind` и сумма подтверждены (SV-054, SV-055). |
| KF-V04 | `VALIDATED` — точных cross-register пар нет (SV-054). |
| KF-V05 | `VALIDATED` — физические ключи/статусы подтверждены; legacy-кратность ПЗ сохранена по BR-018 (SV-058). |
| KF-V06 | `VALIDATED` — ключ, active, период и `Fld6620` подтверждены (SV-056). |
| KF-V07 | `NOT_APPLICABLE` — внешние файлы остаются в Power BI. |
| KF-V08 | `VALIDATED` — текущие Renew и регулярность воспроизводятся; улучшения отмечены отдельно по BR-018 (SV-060). |

Пример KF-V01 (не выполнялся):

```sql
SELECT _RecorderRRef, _LineNo, COUNT(*) AS row_count
FROM _AccumRg7575
GROUP BY _RecorderRRef, _LineNo
HAVING COUNT(*) > 1;
```
