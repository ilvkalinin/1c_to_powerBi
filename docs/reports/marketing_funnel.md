# Требования отчёта: «Воронка»

Статус: `BUSINESS LOGIC COMPLETE / STAGE_2 SOURCE VALIDATION PARTIALLY VALIDATED — SV-080 / IMPLEMENTATION DEFERRED`.

Договорный отчёт № 27, блок «Маркетинг». Он показывает созданные задания
CRM-воронки «Продажа клубной карты», конверсию в активацию подходящих
абонементов и выполнение плана. Источники истины: бизнес-описание, текущие
SQL/M/DAX и Stage 2 source-side evidence. `SV-080` выполнен в gymdb только
на чтение; прочие незапущенные проверки остаются `VALIDATION_PENDING`.

## Бизнес-гранулярность и время

- Задание: одно CRM-задание; количество заданий — `DISTINCTCOUNT(КодЗадания)`.
- Абонемент: текущая модель связывает задание с подходящим активированным
  контрактом через `InfoRg6798`; фактические абонементы считает в контексте
  задания. `SV-080` опроверг one-to-one кардинальность `задание → контракт`.
- Дата задания: дата создания задания. Ею управляются отбор периода, дневные
  графики и сравнение с прошлым годом.
- Дата абонемента: дата активации контракта. Она определяет факт покупки,
  включая накопленный трафик.
- «Накопленный трафик»: задания, созданные в двух календарных месяцах перед
  выбранным месяцем, разделяются на входящий/исходящий по типу первого
  взаимодействия. Текущая DAX исключает из остатка часть закрытых отказом или
  отменой и часть уже активировавших контракт; точные границы и дедупликация
  фиксированы как `VALIDATION_PENDING`.

## Общие фильтры

На страницах «Воронка», «Свод» и «Абонементы»: год/месяц, день, сеть/кластер,
клуб, тип взаимодействия, стаж клиента, возрастная группа купленного
контракта, тип оплаты купленного контракта и длительность купленного
контракта. На «Маркетинговых кампаниях» вместо года/месяца и дня применяется
произвольный период; остаются сеть/кластер, клуб, тип взаимодействия,
маркетинговая кампания, стаж и возрастная группа.

Возрастная группа, тип оплаты и длительность контракта влияют **только** на
метрики, связанные с абонементами; число заданий и его план должны игнорировать
эти срезы. На странице кампаний правило относится к возрастной группе.

## Метрики

| Метрика | Текущее определение | Статус |
|---|---|---|
| Задания факт | distinct созданных заданий в выбранном контексте; contract-срезы удаляются | CONFIRMED current DAX |
| Задания план | сумма дневного плана категорий «Взаимодействия» | CONFIRMED current DAX; внешний набор Power BI |
| Задания факт ПГ | тот же факт за `SAMEPERIODLASTYEAR` | CONFIRMED current DAX |
| Выполнение заданий | `Задания факт / Задания план` | CONFIRMED |
| Выполнение заданий ПГ | `Задания факт / Задания факт ПГ` | CONFIRMED по описанию; название не является plan-vs-PY |
| Дефицит/профицит заданий | `Задания факт − Задания план` | CONFIRMED |
| Абонементы факт | количество подходящих активированных контрактов, атрибутированных заданию | CONFIRMED current; cardinality pending |
| Абонементы план | сумма дневного плана категории «Контракты» | CONFIRMED current DAX; внешний набор Power BI |
| Выполнение абонементов | `Абонементы факт / Абонементы план` | CONFIRMED |
| Дефицит/профицит абонементов | `Абонементы факт − Абонементы план` | CONFIRMED |
| Конверсия план | `Абонементы план / Задания план`; в разрезе используется текущая взвешенная DAX-мера | CONFIRMED current |
| Конверсия факт | `Абонементы факт / Задания факт` | CONFIRMED |
| Отклонение конверсии | `Конверсия факт − Конверсия план` | CONFIRMED |

Планы «Новые планы» и «Подневный план по трафику» — внешние Excel-таблицы
модели Power BI. По решению пользователя 2026-07-30 они остаются в Power BI;
их перенос в PostgreSQL не входит в этот отчёт.

## Страницы и визуализации

| Страница | Содержание |
|---|---|
| «Воронка» | план–факт по заданиям по типу первого взаимодействия; эффективность кампаний; дневной и накопительный графики плана заданий с фактом и ПГ. Типы группируются в «Отдел маркетинга» (входящий) и «Отдел продаж» (исходящий); в обеих группах есть накопленный трафик. |
| «Свод» | та же верхняя воронка; матрица процента выполнения заданий `клуб × тип взаимодействия` с порогами: >100% тёмно-зелёный, 90–100% светло-зелёный, 80–90% жёлтый, <80% красный; рейтинг клубов по выполнению активаций и накопительный график заданий. |
| «Абонементы» | план–факт абонементов по типу взаимодействия, рейтинг клубов по конверсии, дневной и накопительный план–факт активаций, рейтинг клубов по заданиям. |
| «Маркетинговые кампании» | задания, абонементы и конверсия по кампании; структура по типу первого взаимодействия; динамика заданий по дням и месяцам. |
| Страница без ПГ | бизнес-описание называет её повторно «Маркетинговые кампании», а снимок текущей модели — «Для продаж»; по описанию она идентична «Воронке», но без прошлого года. Точное название страницы `UNKNOWN`, на логику данных не влияет. |

## Подтверждённые правила текущего набора

1. Охват задач: воронка `Продажа клубной карты`, задание не помечено на
   удаление, клуб «Детский развивающий центр» исключён, дата создания попадает
   в выгружаемый год.
2. Два exact-значения причины неуспешного завершения «Найдено аналогичное
   задание» исключаются до модели.
3. Контракт выбирается из `InfoRg6798` только при `_Fld6802 = true`; исключены
   «клип-карта» и бесплатный тип оплаты. Текущий SQL ведёт историю активаций
   с 2024-01-01.
4. Возраст: детские секции выделяются по типу контракта; иначе возрастные
   категории номенклатуры переводятся в «Взрослые», «Дети», «Юниоры», а
   неизвестное — «Взрослые». Тип оплаты: «Рекарринг» или «Предоплата».
   Длительность: `001–007`, `008–030`, `031–180`, `181–364`, `365+` дней.
5. Тип первого взаимодействия маппится по фиксированным GUID, затем Power
   Query оставляет отдельными встречу, входящий/исходящий звонок, оформление,
   чат, регистрацию рекомендации и гостевого визита, обратную связь;
   кампанию с родителем, начинающимся на «Промо», относит к мероприятиям,
   остальное — к «Прочим».

## Результат reuse review

Базовый набор отчёта повторно использует `mart.fitness_leads_funnel_task`:
совпадают grain, ключ `task_id`, CRM-источники и task-to-contract enrichment.
Новый факт, копия регистра или отдельное извлечение CRM не нужны. Маркетинговая
классификация первого взаимодействия и меры накопленного трафика остаются
семантикой Power BI поверх этого набора. Решение `REUSE` является проектным до
технической валидации существующего факта.

## Результат Stage 2: SV-080

В контроле текущего bridge после фильтров модели выбраны 100 строк
`InfoRg6798`: они относятся к 36 CRM-заданиям, из которых 21 имеет более
одного контракта. В группах с несколькими заданиями находятся 85 контрактов;
максимум — 16 заданий на контракт. Следовательно, гипотеза one-to-one
`task → contract` имеет статус `VALIDATION_FAILED`.

Это не меняет текущие SQL/M/DAX: task core остаётся `REUSE` из
`mart.fitness_leads_funnel_task` (также опирается на SV-078). До отдельного
решения нельзя получать `contract_count` суммированием строк bridge или
молча дедуплицировать контракты. Никакие объекты PostgreSQL и источника 1С не
создавались и не изменялись.

Детали источников, mapping и проверок — в
[mapping](../mappings/marketing_funnel.md) и
[query review](marketing_funnel_query_review.md).
