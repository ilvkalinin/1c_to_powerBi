# Разбор текущих запросов: «Новички и гостевые визиты»

Статус: `BUSINESS REVIEW COMPLETE / STAGE 2 PARTIALLY VALIDATED — SV-087`.

Источники:

- `Новички и гостевые визиты.docx`, SHA-1 `33f1651add0cca9b245f2f7336767414f7758769` — описание страниц;
- `НОвички и гостевые визиты (2).docx`, SHA-1 `29e83535b03a65f190b227122b1d33f63c4dc60e` — текущие Power Query, SQL и DAX.
- `Pbit_old/Новички и гостевые визиты.pbit`, SHA-256 `3d54a392bec0d3feed21f998c91bf4607886ca101eac7b0adc94d6bbce180796` — архивный PBIT-артефакт; его M не заменяет согласованные current SQL/M/DAX.

NV-V01/NV-V03/NV-V04/NV-V07/NV-V08 выполнены 2026-08-11 в `BEGIN READ ONLY`.
Первичная запись о таймауте исправлена: клиент ошибочно включал SSL, который
сервер не поддерживает. Это не является доказательством отсутствия источника.

## Наборы Power Query

| Набор | Источники | Назначение | Текущая логика |
|---|---|---|---|
| `ПервыеПосещенияNew` | `AccumRg7575`, `Document325`, `Reference59`, клиент, клуб | первые посещения New | `ROW_NUMBER` по контракту, ранний `Period` |
| `Гостевые визиты` | `InfoRg7064`, `Document325`, клиент, клуб, `InfoRg5654`, производные ACCUNIQ/абонементы | гостевая воронка | стаж as-of `_Period`, distinct `(client code, visit date)` |
| `Аккуники` | `AccumRg7575`, клиент, услуга, клуб, контракт | услуги ACCUNIQ | 12 жёстких кодов услуг, затем суммарное количество 1 или 2 |
| `Абонементы` | `Reference59`, клиент, номенклатура, клуб, сотрудник | кандидаты покупок | исключаются бесплатные, клип-карты, ИП и сотрудники; длительность ≥30 |
| `ТурыВсе` / `Туры` | `Reference67`, `Reference106`, `InfoRg7146`, CRM-справочники | встречи | дата из телефонии либо `_Fld820`; воронка «Продажа клубной карты» |
| `ЗаписиНаАккуники` / `ТурыАккуник` | `InfoRg7006`, `Document329`, `Enum448`, услуга | запись на ACCUNIQ для тура | latest только по групповому занятию, исключаются состояния 2/3 |
| `ТурыАбонементы` | `ТурыВсе`, `Абонементы` | покупка после тура | первый контракт клиента в окне 0–44 дня |
| `ПланПоВстречам1` | Google Sheets | месячный план встреч | разворот листа «Апрель - для Ильи план»; структура не закреплена |

## Подтверждённая текущая бизнес-логика

### Первое посещение New

`AccumRg7575` соединяется с `Document325` по recorder и с `Reference59` по
`Fld7578`. Отбираются движения с 2025-01-01, New-контракт, GUID вида
документа «посещение». Ранжирование `ROW_NUMBER(PARTITION BY contract ORDER
BY Period)` оставляет первое движение. После SQL исключаются клубы ДРЦ и УК,
абонементы по тексту «ИП»/«сотрудн» и неклиентский тип контрагента.

### Гостевая конверсия

База гостевых визитов — `InfoRg7064`, соединённый с `Document325` (клуб) и
клиентом. Для каждого клиента M выбирает максимум даты истории стажа до
`_Period`; отсутствие истории подменяется датой 1990-01-01 и стажем `New`.

Архивный PBIT определяет ACCUNIQ по `AccumRg7575` после 2025-01-01, клиенту и коду услуги
`Reference163.Code` из точного набора: `00000017896`, `00000018151`,
`00000017882`, `00000017883`, `00000018152`, `00000017897`, `00000016715`,
`00000016162`, `00000016194`, `00000016161`, `00000017672`, `00000016160`.
M заменяет `Fld7585 = -1` на −1, любое иное значение на +1, группирует по
`period × club × service code × client code` и оставляет итог `1` или `2`.
`АккуникиВгв` делает inner join по `client code × date`: гость получает
ACCUNIQ только при равенстве дат. Это потенциальный артефакт до сравнения с
согласованным current запросом. Текущий blocker списка услуг и правила выбора
не снимается. Покупка — первый по смещению
подходящий абонемент клиента, активированный в `[guest_visit_date,
guest_visit_date + 44 days]`. Финальный `Distinct` оставляет одну строку на
`client code × guest visit date`.

### Туры

`ТурыВсе` оставляет CRM-взаимодействия задач в воронке «Продажа клубной
карты» с GUID события «встреча». `ДатаДляОтчета` равна `_InfoRg7146.Fld7150`,
если она не `NULL`, иначе `_Reference67.Fld820`; подзапрос не агрегирует
телефонию. `Туры` сохраняет только пары «Закрыто/Выполнено» и
«Запланировано/Не выполнено». Успешность покупки и запись на ACCUNIQ затем
определяются двумя M-join; оба признака остаются пустыми вне совпадения.

## DAX: что остаётся в модели

- distinct-счётчики и конверсии должны оставаться DAX-мерами, потому что
  зависят от фильтров периода;
- текущие `_ТуровСостоялось` и `_ТуровЗапланировано` используют `COUNT`, не
  `DISTINCTCOUNT` stable ID — сохранить как текущую семантику до V-08;
- возрастные группы 0–2, 3–5, …, 82–85, 85+ и крупная группа дети `<14` /
  взрослые `14+` считаются в DAX от возраста на дату события;
- `План на день`, `План (НИ)` и `Факт (НИ)` остаются DAX: они используют
  `TODAY()` и контекст календаря.

## Риски реализации

1. `ROW_NUMBER` первого визита не имеет tie-breaker и не проверяет знак/
   `Active` движения.
2. Гостевой запрос не использует `Статус` и `РегистрацияГостевогоВизита`, хотя
   их извлекает. Ни одно поле не доказывает факт проведения.
3. Левый join с историей стажа после фильтра по максимуму может размножить
   гостевые строки при равных периодах.
4. Фильтрация ACCUNIQ по `Количество IN (1,2)` может исключать корректные
   повторные/сторнирующие события; бизнес-смысл `Fld7585` не доказан.
5. Покупка присоединяется по коду клиента, а не по физическому ID; уникальность
   кода и допустимость повторных визитов в одном дне не доказаны.
6. `ЗаписиНаАккуники` выбирает latest record по `ГрупповоеЗанятие`, не по
   клиенту; это противоречит последующему join по клиенту и дате.
7. Состояние тура названо текстом из справочника, а статус — жёсткими GUID:
   пара требует проверки на реальных значениях.
8. Внешний план привязан к имени листа одного месяца и не имеет стабильного
   ключа/схемы.

## Подготовленные проверки STAGE_2

NV-V01/NV-V03/NV-V04/NV-V07/NV-V08 выполнены и описаны в SV-087. Архивный
PBIT сохранил возможные ACCUNIQ-константы и правило выбора, но не подтверждает
current-M. V-02/V-05/V-06/V-09 остаются `VALIDATION_PENDING`.

| ID | Проверка и ожидаемый результат |
|---|---|
| V-01 | Metadata/`NULL`/уникальность `Reference59.ID`, `Reference67.ID`, `Document325.ID`; ожидается один ID = одна сущность. |
| V-02 | Кардинальность `AccumRg7575 → Document325` и `AccumRg7575.Fld7578 → Reference59`; ожидается отсутствие размножения первого-визита при join. |
| V-03 | Распределение `InfoRg7064.Fld7067`, `Fld7066`, наличие recorder/client/date; ожидается явно выбранный набор проведённых гостевых визитов. |
| V-04 | Проверить `(guest registration, client, visit date)` и `(client, visit date)` на дубли; ожидается выбранный стабильный ключ без скрытого `Distinct`. |
| V-05 | Сверить 12 кодов ACCUNIQ с `Reference163`, `RecordKind`, `Active`, `Fld7585`; ожидается подтверждённая единица и набор услуг. |
| V-06 | На контрольных клиентах проверить 0, 44 и 45 дней после визита/тура; ожидается включение 0 и 44, исключение 45. |
| V-07 | Проверить as-of стаж `InfoRg5654`: tie-break, отсутствие записи, равенство периодов; ожидается отсутствие неподтверждённого default `New`. |
| V-08 | Кардинальность `Reference67 → InfoRg7146`, значения состояния/статуса и фильтров воронки/типа; ожидается одна строка тура на `Reference67.ID` после source-side агрегации. |
| V-09 | Проверить `InfoRg7006` по client × training × date, latest state и значения 2/3; ожидается корректная атрибуция ACCUNIQ без потери клиентов. |

Пример V-04 (адаптировать имена полей после V-01):

```sql
SELECT
  g._Fld7066RRef AS guest_registration_id,
  g._Fld7065RRef AS client_id,
  g._Fld7068::date AS guest_visit_date,
  COUNT(*) AS row_count
FROM _InfoRg7064 AS g
GROUP BY 1, 2, 3
HAVING COUNT(*) > 1;
```

Ожидаемый результат: дубли либо отсутствуют, либо для каждого типа дубля
зафиксировано правило выбора актуальной записи; иначе `VALIDATION_FAILED`.

## Подготовленный read-only набор

NV-V01/NV-V03/NV-V04/NV-V07/NV-V08 зафиксированы до запуска в
[`newcomer_guest_visits_2026-08-11.sql`](../source_metadata/validation_sql/newcomer_guest_visits_2026-08-11.sql).
NV-V03/V04 подтвердили unique technical key, но material candidate duplicates;
NV-V07 — отсутствие history ties; NV-V08 — отсутствие phone-row multiplication
в текущем tour-контуре. Архивный PBIT сохранён как potential artifact, но не
снимает отсутствие current-M констант ACCUNIQ; V-02/V-05/V-06/V-09 остаются
`VALIDATION_PENDING`.
