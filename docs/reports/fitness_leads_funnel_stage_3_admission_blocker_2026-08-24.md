# Admission blocker: «Воронка лиды фитнес»

Статус: `BLOCKER`.

The planning-only review confirmed the separate task-fact design but cannot
review implementation SQL. Current task outcomes depend on unvalidated
physical joins and client-code/date attribution; using the implemented
marketing mart would silently change its one-funnel contract.

The blocker and the exact required read-only controls are recorded in
[fitness_leads_funnel_stage_3_planning.md](fitness_leads_funnel_stage_3_planning.md).
No target object, source query, DDL, DML or Power BI artifact was created or
changed.
